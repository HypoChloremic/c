# Basic stuff the compiler does

basic c compilation steps:
1. preprocessing: edits the program just prior to compilation
2. compiler -> checks for errors -> object code (machine instructions)
3. assembler
4. linker
